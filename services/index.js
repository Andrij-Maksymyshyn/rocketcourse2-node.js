const {
  hashPassword,
  checkPasswords,
  generateAccessTokenPair,
  validateToken,
  validateAccessToken,
} = require("./oauthService");

module.exports = {
  hashPassword,
  checkPasswords,
  generateAccessTokenPair,
  validateToken,
  validateAccessToken,
};
